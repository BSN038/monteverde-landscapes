/* =========================================================
   TrustBar – Monteverde Landscapes
   ========================================================= */

export default function TrustBar() {
  return (
    <section>
      <div className="container">
        <p>Trusted by homeowners across the region.</p>
      </div>
    </section>
  );
}
